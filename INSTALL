Requirements
------------
	Python 2.2 or greater
	GCC
	LibXML requirements and associated Python bindings
	XML Security Library version >= 1.2.x
	OpenSSL or GnuTLS and Libgcrypt or NSS (Mozilla cryptographic library)

To build, install and clean sources
-----------------------------------
	./setup.py

The install process requires root access.
